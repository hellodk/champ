node_modules/
/dist/
out/
.vscode-test/
*.vsix
.aidev/
.champ/sessions/
test-reports/
.DS_Store
.env
.env.*
*.map

# webview-ui/dist/ is hand-written vanilla JS source, not a build artifact,
# so it is intentionally tracked (not ignored).
